
version: 2

sources:
    - name: raw
      schema : gz_raw_data_290725
      tables:
      - name: product
        identifier: raw_gz_product
        description: list of products with unit price
      - name: sales
        identifier: raw_gz_sales
        description: sales of greenweez / we have one row per product id found in each orders_id
      - name: ship
        identifier: raw_gz_ship
        description: ship for every orders_id with log cost and ship cost for every row/orders_id